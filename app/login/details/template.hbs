<h4><strong class="brand-primary-text">Almost there,</strong> update your details</h4>
<form id="login-update-details-form" role="form" {{action "submit" on="submit"}}>
  <div class="form-group">
    {{input type="password" class="form-control input-lg" id="details-password" value=password placeholder="Choose a password"}}
  </div>
  <div class="form-group">
    {{input type="password" class="form-control input-lg" id="details-password-confirmation" value=passwordConfirmation placeholder="Confirm your password"}}
  </div>
  <div class="form-group input-group">
    <span class="input-group-addon"><i class="ti-{{if useUsername 'user brand-primary-text' 'email'}}"></i></span>
    {{input type=(if useUsername 'text' 'email') class="form-control input-lg" id="details-identifier" value=identifier placeholder=(if useUsername 'Enter username' 'Enter email address')}}
  </div>
  <div class="checkbox margin-bottom {{if useUsername 'checked'}}">
    <label for="use-username">
      {{input type="checkbox" id="use-username" checked=useUsername}}
      I do not have an email address
    </label>
  </div>
  <button type="submit" class="btn btn-brand btn-lg btn-block" disabled={{dataInvalid}}>Log in</button>
</form>
<p><i class="ti-check-box brand-primary-text"></i> Make a note of your username</p>
<p><i class="ti-check-box brand-primary-text"></i> Make a note of your password</p>
<p><i class="ti-check-box brand-primary-text"></i> Keep it somewhere safe</p>
